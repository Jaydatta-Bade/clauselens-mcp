# ClauseLens runs as a public MCP server — no auth or secrets required.
# All variables below are optional; the defaults shown are production-ready.

# Rate limiting (per client IP, fixed window)
RATELIMIT_REQUESTS=60
RATELIMIT_WINDOW_SECONDS=3600

# Server port (Railway sets this automatically in production)
PORT=8000
